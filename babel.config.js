// Used by Jest only — react-native-builder-bob compiles lib/ with its own preset.
module.exports = {
    presets: ['module:@react-native/babel-preset'],
    plugins: ['react-native-reanimated/plugin'],
};
