import { SwipeListView, SwipeRow } from '../index';

describe('public exports', () => {
    it('exports SwipeListView and SwipeRow', () => {
        expect(SwipeListView).toBeDefined();
        expect(SwipeRow).toBeDefined();
    });
});
