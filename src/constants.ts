// Values copied verbatim from v3 components/SwipeRow.js.

/** Default delay before the preview animation starts (ms). */
export const DEFAULT_PREVIEW_OPEN_DELAY = 700;

/** Delay between the preview open and close animations (ms). */
export const PREVIEW_CLOSE_DELAY = 300;

/** Clamp for the velocity term in the swipe-release calculation. */
export const MAX_VELOCITY_CONTRIBUTION = 5;

/** Safety timer before parent scroll is force re-enabled after a gesture (ms). */
export const SCROLL_LOCK_MILLISECONDS = 300;
