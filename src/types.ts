// Placeholder — full public API types arrive in Phase 2.
import type { ReactNode } from 'react';

export interface SwipeRowProps {
    children?: ReactNode;
}

export interface SwipeListViewProps {
    children?: ReactNode;
}
